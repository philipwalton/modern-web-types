// Generated by modern-web-types from TypeScript-DOM-lib-generator
// (upstream a193ecb2b75a) — do not edit.
// Scope: window. Spec: credential-management

interface CredentialData {
    id: string;
}

interface FederatedCredentialInit extends CredentialData {
    iconURL?: string;
    name?: string;
    origin: string;
    protocol?: string;
    provider: string;
}

interface PasswordCredentialData extends CredentialData {
    iconURL?: string;
    name?: string;
    origin: string;
    password: string;
}

/** Available only in secure contexts. */
interface CredentialUserData {
    readonly iconURL: string;
    readonly name: string;
}

/**
 * The **`FederatedCredential`** interface of the Credential Management API provides information about credentials from a federated identity provider. A federated identity provider is an entity that a website trusts to correctly authenticate a user, and that provides an API for that purpose. OpenID Connect is an example of a federated identity provider framework.
 * Available only in secure contexts.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FederatedCredential)
 */
interface FederatedCredential extends Credential, CredentialUserData {
    /**
     * The **`protocol`** property of the FederatedCredential interface returns a read-only string containing a credential's federated identity protocol. If this property is null, the protocol may be inferred from the FederatedCredential.provider property.
     *
     * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FederatedCredential/protocol)
     */
    readonly protocol: string | null;
    /**
     * The **`provider`** property of the FederatedCredential interface returns a string containing a credential's federated identity provider.
     *
     * [MDN Reference](https://developer.mozilla.org/docs/Web/API/FederatedCredential/provider)
     */
    readonly provider: string;
}

declare var FederatedCredential: {
    prototype: FederatedCredential;
    new(data: FederatedCredentialInit): FederatedCredential;
};

/**
 * The **`PasswordCredential`** interface of the Credential Management API provides information about a username/password pair. In supporting browsers an instance of this class may be passed in the credential member of the init object for global fetch().
 * Available only in secure contexts.
 *
 * [MDN Reference](https://developer.mozilla.org/docs/Web/API/PasswordCredential)
 */
interface PasswordCredential extends Credential, CredentialUserData {
    /**
     * The **`password`** read-only property of the PasswordCredential interface returns a string containing the password of the credential.
     *
     * [MDN Reference](https://developer.mozilla.org/docs/Web/API/PasswordCredential/password)
     */
    readonly password: string;
}

declare var PasswordCredential: {
    prototype: PasswordCredential;
    new(form: HTMLFormElement): PasswordCredential;
    new(data: PasswordCredentialData): PasswordCredential;
};
